var canvas = document.getElementById("canvas");
var ctx = canvas.getContext("2d");

class Car {
  constructor() {
    this.linkImg = linkImg;
  }

  // method
  drawCar() {
    return ctx.drawImage(
      car_image,
      450,
      850,
      car_image.width / 4,
      car_image.height / 4
    );
  }
  getLinkImgCar() {
    return (this.linkImgCar = linkImg);
  }
  drawStar(){
    return this.
  }
}
